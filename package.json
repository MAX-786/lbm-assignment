{
  "name": "root-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start --prefix frontend\" \"npm run start --prefix backend\"",
    "install-deps": "concurrently \"npm install --prefix frontend\" \"npm install --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
